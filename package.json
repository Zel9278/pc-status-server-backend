{
    "scripts": {
        "build": "tsc",
        "test": "tsc && node dist/.",
        "pretty": "prettier --write ."
    },
    "devDependencies": {
        "prettier": "^2.8.1",
        "typescript": "^4.9.4"
    },
    "dependencies": {
        "socket.io": "^4.5.4"
    }
}
